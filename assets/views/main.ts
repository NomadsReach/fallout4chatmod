// FalloutChat Entry Point
// CRITICAL: Import store FIRST so window functions are registered before app logic runs

import './store';
import './chat';

console.log('[FalloutChat] Bundle loaded and initialized');
